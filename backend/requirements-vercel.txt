# AgroShield Backend Requirements - Vercel Optimized (Lightweight)

# Core dependencies
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
pydantic>=2.4.0
Pillow>=10.1.0
numpy>=1.24.0
requests>=2.31.0

# Authentication
supabase>=2.0.0
pyjwt>=2.8.0

# Payment Integration
stripe>=7.4.0
python-dotenv>=1.0.0

# Lightweight ML (instead of TensorFlow)
scikit-learn>=1.3.0
pandas>=2.1.0

# Image processing (headless version for serverless)
opencv-python-headless>=4.8.0

# SMS gateway
twilio>=8.9.0

# Note: TensorFlow and heavy ML dependencies removed for Vercel deployment
# For AI features, use external ML APIs or lightweight models
